# Decisions log

> Append-only. The agent appends; it never edits or deletes prior
> entries. Format: `## YYYY-MM-DD  <scope>  <command>` followed by
> 2–6 bullet lines.
>
> When this file exceeds ~2K lines, move it manually to
> `.forge/decisions/<YYYY>.md` and start a fresh `decisions.md`.
> `/forge.scan` reads both the current file and any rotated archives.

## INIT  (placeholder — replaced by `/forge.init`)
- forge installed. Constitution v1.0.0 (template) in place.
- Awaiting first `/forge.init` to synthesise constitution from codebase.
